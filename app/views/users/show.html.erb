<h1>Greetings!</h1>

<div class="media-body">
  <h2 class="media-heading"><%= @user.email %></h2>

  <%= render @user.items %>

  <div class="media">
    <%= render 'items/form', item: Item.new, user: @user %>
  </div>
</div>
